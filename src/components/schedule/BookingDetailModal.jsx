/**
 * @deprecated
 *
 * Booking detail UI moved to the shared BookingDetailDrawer during Phase 3B.
 * Keep this compatibility alias temporarily so any remaining legacy import
 * still resolves while consumers migrate.
 */
export { default } from '../booking/BookingDetailDrawer.jsx';
